//////////
/// Engine project

- The idea is to create a game, having fun along the process, and just live life, with the possibility of leaving some legacy of fun, joy and great moments for the players and earning money along the process.


////////////////
///// GAME /////
////////////////

-> Asset Manager, data folder access, etc.
-> log name when pushing arena.

-> Create battle world.

////// Battle mode //////

DISCUSS:
-> Component architecture: pools of component types, and then we assign them to the actors as they have it.



-> create a map with isometric view
	-> imported actors - review pipeline.
			-> create the actor manager.
				-> create a max actors per scene, in this case same as Meshes
				-> Decouple the texture creation on the GPU, and create texture manager.
			


-> being able to navigate through it.
-> add collisions to the map -> should be just a plain cube, no abrupt terrain, or complex terrain.
-> crate a character with a collision.
-> add a mock to the character sprite.
-> being able to move the character around.

FINAL: having the map in isometric view, and being able to move the mock character around.

/////
-> Make a consistent battle mode.
	-> Create units and be able to click and move.
		-> Be able to render a map
		-> Characters with collisions to move around it.
			-> Collisions, 2D sprite as quad, but we have a collision shape.



		-> Grid.
		-> AI movement.
	-> Cards 
		-> Maybe we can start with a mock for the cards and effects.
		-> Cards with UI
			-> UI framework.
- Create the world with isometric view



//////////////////////////
- Render thread:
	- Take a look at the implementation of the mailbox and see if it can be improved.
	- Create the one frame delay between render thread and game thread.

- Create the Texture Manager.
- Create handle for objects to not carry ptrs?

////////////////
//// DAY
///////////////

- Crate the camera free movement, through the mouse.
- Create the infinite scenario.


////////////////////////////////


RENDERER:



WINDOWS:
	- Use direct3D 12 for the graphics:
		- render pixel.
		- render line.
		- render a mesh.


MACOS: 
	- same setup for windows but with METAL as the graphics API.


POST IMPLEMENTATION:
- REFACTOR - Separate the engine and the application.


STRING
- Create a string_t table to check if we have already that string in memory.


PLATFORM:
- Crate a page size function to extract the page size.


C:\Program Files (x86)\Windows Kits\10\Lib\10.0.22621.0\um\x86